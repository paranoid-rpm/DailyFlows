package com.example.dailyflows

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * Legacy Kotlin activity placeholder.
 *
 * The app entry point is Java: com.example.dailyflows.ui.MainActivity.
 * This file exists only to prevent build errors if the project was created with Kotlin templates.
 */
class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
